<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.cart">
	<resultMap id="cartResult" type="cartVO">
		<result property="cartId" column="cartId" />
        <result property="id" column="id" />
        <result property="foodKey" column="foodKey" />
        <result property="foodName" column="foodName" />
        <result property="foodImg" column="foodImg" />
        <result property="foodPrice" column="foodPrice" />
        <result property="foodCount" column="foodCount" />
        <result property="addedPrice" column="addedPrice" />
	</resultMap> 
		
	<select id="selectCartList" parameterType="cartVO" resultMap="cartResult">
      <![CDATA[
         select * from cart 
         where id=#{id}
      ]]>
	</select>
    
  <insert id="insertCart"  parameterType="cartVO">
		<![CDATA[
		 insert into cart(cartId, id, foodKey, foodName, foodImg, foodPrice, foodCount, addedPrice)
		 values(#{cartId}, #{id}, #{foodKey}, #{foodName}, #{foodImg}, #{foodPrice}, #{foodCount}, #{addedPrice})
		]]>      
	</insert>
     
   <delete id="deleteCart"  parameterType="cartVO">
	<![CDATA[
	   delete from cart
	   where
	   id=#{id}
	]]>      
  </delete>
  
     <delete id="deleteSingleCart"  parameterType="cartVO">
	<![CDATA[
	   delete from cart
	   where
	   cartId=#{cartId}
	]]>      
  </delete>
  
</mapper>